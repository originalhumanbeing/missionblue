# from sqlalchemy import *
#
#
# class Base:
#     id = Column('id', Integer, primary_key=True)
#     created = Column(DateTime(timezone=True), server_default=func.utcnow())
#     modified = Column(DateTime(timezone=True), onupdate=func.utcnow())
